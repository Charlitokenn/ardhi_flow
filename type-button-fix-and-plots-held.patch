diff --git a/src/client/components/forms/contacts/view-contact-form.tsx b/src/client/components/forms/contacts/view-contact-form.tsx
index 32e0c1a..39c9cbf 100644
--- a/src/client/components/forms/contacts/view-contact-form.tsx
+++ b/src/client/components/forms/contacts/view-contact-form.tsx
@@ -34,6 +34,7 @@ import {CustomTabsHorizontal, type HorizontalTabItem,} from "@/components/custom
 import {Avatar, AvatarBadge, AvatarFallback, AvatarImage,} from "@/components/ui/avatar.tsx";
 import {Badge} from "@/components/ui/badge.tsx";
 import {ContactSection, DetailItem,} from "@/components/views/contact-overview.tsx";
+import {PlotsHeldDataGrid} from "@/components/data-grids/plots-held-datagrid.tsx";
 
 // ---------------------------------------------------------------------------
 // Constants
@@ -292,6 +293,14 @@ function PersonalParticularsContent({contact}: { contact: ClientContact }) {
     );
 }
 
+function PlotsHeldTabContent({contact}: { contact: ClientContact }) {
+    return (
+        <ContactSection title="Plots Held">
+            <PlotsHeldDataGrid plots={contact.plots}/>
+        </ContactSection>
+    );
+}
+
 function PdfTabHeader({
                           plotSelector,
                           document,
@@ -595,7 +604,13 @@ export const ViewContactForm = ({
         const roleTab = (id: string, label: string, icon: LucideIcon) =>
             tabs.push({id, label, icon, content: <PlaceholderTabContent/>});
 
-        if (hasPlots && isClient) roleTab("plots-held", "Plots Held", MapPlusIcon);
+        if (hasPlots && isClient)
+            tabs.push({
+                id: "plots-held",
+                label: "Plots Held",
+                icon: MapPlusIcon,
+                content: <PlotsHeldTabContent contact={contact}/>,
+            });
         if (isSupplier)
             roleTab("supplier-projects", "Supplier Projects", MapPlusIcon);
         if (isAgent) {
diff --git a/src/client/components/reui/data-grid/data-grid-column-filter.tsx b/src/client/components/reui/data-grid/data-grid-column-filter.tsx
index 32acc41..7999808 100644
--- a/src/client/components/reui/data-grid/data-grid-column-filter.tsx
+++ b/src/client/components/reui/data-grid/data-grid-column-filter.tsx
@@ -48,7 +48,7 @@ function DataGridColumnFilter<TData extends object, TValue>({
   return (
     <Popover>
       <PopoverTrigger asChild>
-        <Button variant="outline" size="sm">
+        <Button type="button" variant="outline" size="sm">
           <CirclePlusIcon className="size-4" />
           {title}
           {selectedValues?.size > 0 && (
diff --git a/src/client/components/reui/data-grid/data-grid-column-header.tsx b/src/client/components/reui/data-grid/data-grid-column-header.tsx
index f41af8d..d5ce24d 100644
--- a/src/client/components/reui/data-grid/data-grid-column-header.tsx
+++ b/src/client/components/reui/data-grid/data-grid-column-header.tsx
@@ -293,6 +293,7 @@ function DataGridColumnHeaderInner<TData extends object, TValue>({
         <DropdownMenu>
           <DropdownMenuTrigger asChild>
             <Button
+              type="button"
               variant="ghost"
               className={headerButtonClassName}
               disabled={isLoading}
@@ -308,6 +309,7 @@ function DataGridColumnHeaderInner<TData extends object, TValue>({
         </DropdownMenu>
         {props.tableLayout?.columnsPinnable && canPin && isPinned && (
           <Button
+            type="button"
             size="icon-sm"
             variant="ghost"
             className="rounded-md -me-1 size-7"
@@ -326,6 +328,7 @@ function DataGridColumnHeaderInner<TData extends object, TValue>({
     return (
       <div className="-ms-2 flex h-full items-center">
         <Button
+          type="button"
           variant="ghost"
           className={headerButtonClassName}
           disabled={isLoading}
diff --git a/src/client/components/reui/data-grid/data-grid-pagination.tsx b/src/client/components/reui/data-grid/data-grid-pagination.tsx
index 6e586c5..33560d0 100644
--- a/src/client/components/reui/data-grid/data-grid-pagination.tsx
+++ b/src/client/components/reui/data-grid/data-grid-pagination.tsx
@@ -81,6 +81,7 @@ function DataGridPagination(props: DataGridPaginationProps): JSX.Element {
       buttons.push(
         <Button
           key={i}
+          type="button"
           size="icon-sm"
           variant="ghost"
           className={cn(btnBaseClasses, "text-muted-foreground", {
@@ -104,6 +105,7 @@ function DataGridPagination(props: DataGridPaginationProps): JSX.Element {
     if (currentGroupStart > 0) {
       return (
         <Button
+          type="button"
           size="icon-sm"
           className={btnBaseClasses}
           variant="ghost"
@@ -121,6 +123,7 @@ function DataGridPagination(props: DataGridPaginationProps): JSX.Element {
     if (currentGroupEnd < pageCount) {
       return (
         <Button
+          type="button"
           className={btnBaseClasses}
           variant="ghost"
           size="icon-sm"
@@ -185,6 +188,7 @@ function DataGridPagination(props: DataGridPaginationProps): JSX.Element {
             {pageCount > 1 && (
               <div className="order-1 flex items-center space-x-1">
                 <Button
+                  type="button"
                   size="icon-sm"
                   variant="ghost"
                   className={btnArrowClasses}
@@ -204,6 +208,7 @@ function DataGridPagination(props: DataGridPaginationProps): JSX.Element {
                 {renderEllipsisNextButton()}
 
                 <Button
+                  type="button"
                   size="icon-sm"
                   variant="ghost"
                   className={btnArrowClasses}
diff --git a/src/client/components/reui/data-grid/data-grid-table-dnd-rows.tsx b/src/client/components/reui/data-grid/data-grid-table-dnd-rows.tsx
index 665d594..9ec2017 100644
--- a/src/client/components/reui/data-grid/data-grid-table-dnd-rows.tsx
+++ b/src/client/components/reui/data-grid/data-grid-table-dnd-rows.tsx
@@ -122,6 +122,7 @@ function DataGridTableDndRowHandle({
   if (!context || disabled) {
     return (
       <Button
+        type="button"
         variant="ghost"
         size="icon-sm"
         className={cn(
@@ -143,6 +144,7 @@ function DataGridTableDndRowHandle({
 
   return (
     <Button
+      type="button"
       variant="ghost"
       size="icon-sm"
       className={cn(
diff --git a/src/client/components/reui/data-grid/data-grid-table-dnd.tsx b/src/client/components/reui/data-grid/data-grid-table-dnd.tsx
index 76ba1c7..106ebf9 100644
--- a/src/client/components/reui/data-grid/data-grid-table-dnd.tsx
+++ b/src/client/components/reui/data-grid/data-grid-table-dnd.tsx
@@ -109,6 +109,7 @@ function DataGridTableDndHeader<TData extends object>({
       <div className="flex items-center justify-start gap-0.5">
         {canOrder && (
           <Button
+            type="button"
             size="icon-sm"
             variant="ghost"
             className={`-ms-2 size-6 ${isDragging ? "cursor-grabbing" : "cursor-grab active:cursor-grabbing"}`}
